stellar_core:
    image: sgehrman/stellar-core
    command: --testnet
    container_name: stellar-core
    volumes:
      - /home/steve/stellar-test:/opt/stellar
    restart: unless-stopped
    environment:
      POSTGRES_PASSWORD: password
    ports:
      - 8000:8000/tcp
