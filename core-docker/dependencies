# dependencies
apt-get update
apt-get install -y curl git libsqlite3-dev libsasl2-dev postgresql-client postgresql postgresql-contrib sudo vim python ca-certificates software-properties-common
apt-get clean

echo "\nDone installing dependencies...\n"
